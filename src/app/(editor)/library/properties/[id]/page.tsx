// Route handled by (dashboard)/library/properties/[id]/page.tsx
// This file exists to satisfy the directory structure but must not export a default
// component, otherwise Next.js will report a duplicate-route build error.
export {}
