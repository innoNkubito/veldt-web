// Placeholder — no active routes live under (editor) currently.
import type { ReactNode } from 'react'
export default function EditorLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
